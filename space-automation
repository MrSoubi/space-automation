#!/usr/bin/env bash
# Launch Space Automation: builds if needed and runs the interactive TUI host.
# Options: --save path  --script path  --budget n  --paused  --plain
set -euo pipefail
cd "$(dirname "$0")"
exec dotnet run --project backend/SpaceAutomation.Host -- "$@"
